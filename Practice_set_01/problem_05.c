#include <stdio.h>

int main(){
    int a = 5, A;
    A = a*a;
    printf("Area of the square A is: %d\n" , A);
    float side = 5.5, Area;
    Area = side*side;
    printf("Area of the sqaure is:%.2f", Area);
    return 0;
}